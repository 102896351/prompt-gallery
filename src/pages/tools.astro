---
import Base from '../layouts/Base.astro';
import { tools, toolCategories, getAllTags } from '../data/tools';

const base = import.meta.env.BASE_URL.replace(/\/$/, '');
const tags = getAllTags();
---

<Base title="AI Tools" description="A curated list of the best AI tools for image, chat, dev, and more.">
  <section class="hero tools-hero">
    <div class="container">
      <h1><span class="grad">AI Tools</span></h1>
      <p>A hand-picked directory of the best AI tools for image generation, chat, coding, and creative workflows.</p>
    </div>
  </section>

  <section class="section">
    <div class="container">
      <!-- Tag filter -->
      <div class="pills" id="tool-filter-pills">
        <button class="pill active" data-tag="all">All</button>
        {tags.map((tag) => (
          <button class="pill" data-tag={tag}>{tag}</button>
        ))}
      </div>

      <!-- Sections by category -->
      {toolCategories.map((cat) => {
        const catTools = tools.filter((t) => t.category === cat.id);
        if (catTools.length === 0) return null;
        return (
          <div class="tool-section" data-section>
            <div class="section-head">
              <h2>{cat.label}</h2>
              <span class="count">{catTools.length} tools</span>
            </div>
            <div class="grid tool-grid">
              {catTools.map((tool) => (
                <a
                  href={tool.url}
                  target="_blank"
                  rel="noopener"
                  class="card tool-card"
                  data-tags={tool.tags.join(' ')}
                  data-name={tool.name.toLowerCase()}
                >
                  <div class="tool-card-head">
                    {tool.icon && (
                      <img
                        src={tool.icon.startsWith('http') ? tool.icon : `${base}${tool.icon}`}
                        alt=""
                        class="tool-icon"
                        width="32"
                        height="32"
                        loading="lazy"
                        decoding="async"
                        onerror="this.style.visibility='hidden'"
                      />
                    )}
                    <h3>{tool.name}</h3>
                    {tool.featured && <span class="tool-star" title="Featured">★</span>}
                  </div>
                  <p class="tagline">{tool.description}</p>
                  <div class="card-meta">
                    {tool.tags.slice(0, 3).map((tag) => (
                      <span class="badge">{tag}</span>
                    ))}
                  </div>
                </a>
              ))}
            </div>
          </div>
        );
      })}

      <p class="empty hidden" id="tool-empty">No tools match this filter.</p>
    </div>
  </section>
</Base>

<style>
  .tools-hero { padding-bottom: 32px; }
  .tools-hero h1 {
    font-size: clamp(32px, 4.5vw, 44px);
    margin: 0 0 12px;
    font-weight: 800;
    letter-spacing: -0.02em;
  }
  .tools-hero p {
    color: var(--text-soft);
    max-width: 640px;
    margin: 0 auto;
  }
  .tool-section { margin-bottom: 48px; }
  .tool-section .section-head { margin-bottom: 16px; }
  .tool-card {
    display: flex;
    flex-direction: column;
    padding: 16px 18px;
  }
  .tool-card-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    margin-bottom: 8px;
  }
  .tool-card-head h3 {
    margin: 0;
    font-size: 16px;
    font-weight: 600;
    flex: 1;
    min-width: 0;
  }
  .tool-icon {
    flex-shrink: 0;
    width: 32px;
    height: 32px;
    border-radius: 8px;
    object-fit: contain;
    background: var(--bg-soft);
    padding: 4px;
    box-sizing: border-box;
  }
  .tool-star {
    color: var(--amber);
    font-size: 14px;
  }
  .tool-card .tagline {
    flex: 1;
    margin: 0 0 12px;
    font-size: 13px;
    line-height: 1.55;
  }
  .tool-card .card-meta {
    margin-top: auto;
  }
</style>

<script is:inline>
  (function () {
    const pills = document.querySelectorAll('#tool-filter-pills .pill');
    const sections = document.querySelectorAll('[data-section]');
    const empty = document.getElementById('tool-empty');
    if (!pills.length) return;

    let activeTag = 'all';

    function apply() {
      let visibleTotal = 0;
      sections.forEach((section) => {
        const cards = section.querySelectorAll('.tool-card');
        let visibleInSection = 0;
        cards.forEach((card) => {
          const tags = card.dataset.tags || '';
          const match = activeTag === 'all' || tags.split(' ').includes(activeTag);
          card.style.display = match ? '' : 'none';
          if (match) { visibleInSection++; visibleTotal++; }
        });
        // 隐藏空的 section（标题也藏）
        section.style.display = visibleInSection > 0 ? '' : 'none';
      });
      if (empty) empty.classList.toggle('hidden', visibleTotal > 0);
    }

    pills.forEach((p) => {
      p.addEventListener('click', () => {
        pills.forEach((q) => q.classList.remove('active'));
        p.classList.add('active');
        activeTag = p.dataset.tag;
        apply();
      });
    });
  })();
</script>